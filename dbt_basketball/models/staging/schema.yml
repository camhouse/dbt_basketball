
version: 2

models:
  - name: stg_season_stats
    description: "Select all season stats"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

